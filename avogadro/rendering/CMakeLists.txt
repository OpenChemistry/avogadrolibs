find_package(Eigen3 REQUIRED)
# Add as "system headers" to avoid warnings generated by them with
# compilers that support that notion.
include_directories(SYSTEM ${EIGEN3_INCLUDE_DIR})

find_package(OpenGL REQUIRED)
find_package(GLEW REQUIRED)
include_directories(SYSTEM ${OPENGL_INCLUDE_DIR} ${GLEW_INCLUDE_DIRS})

set(HEADERS
  avogadrogl.h
  bufferobject.h
  camera.h
  glrenderer.h
  scene.h
  shader.h
  shaderprogram.h
)

set(SOURCES
  bufferobject.cpp
  camera.cpp
  glrenderer.cpp
  scene.cpp
  shader.cpp
  shaderprogram.cpp
)

avogadro_add_library(AvogadroRendering ${HEADERS} ${SOURCES})
target_link_libraries(AvogadroRendering
  ${GLEW_LIBRARY}
  ${OPENGL_LIBRARIES})
